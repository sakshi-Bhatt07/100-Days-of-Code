Q141: Define a structure Student with name, roll_no, and marks, then read and print one student's data.

/*
Sample Test Cases:
Input 1:
Name: Asha
Roll: 101
Marks: 90
Output 1:
Name: Asha | Roll: 101 | Marks: 90

*/
#include <stdio.h>

// Define the Student structure
typedef struct {
    char name[50];
    int roll_no;
    int marks;
} Student;

int main() {
    Student s;

    // Read student data
    printf("Enter Name: ");
    scanf("%49s", s.name);  // %49s to prevent buffer overflow

    printf("Enter Roll: ");
    scanf("%d", &s.roll_no);

    printf("Enter Marks: ");
    scanf("%d", &s.marks);

    // Pr
  
