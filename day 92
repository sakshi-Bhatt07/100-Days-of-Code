Q142: Store details of 5 students in an array of structures and print all.

/*
Sample Test Cases:
Input 1:
Details of 5 students (Name, Roll, Marks)
Output 1:
Tabular list of all 5 students with their details

*/
#include <stdio.h>

// Define the Student structure
typedef struct {
    char name[50];
    int roll_no;
    int marks;
} Student;

int main() {
    Student students[5];  // Array to store 5 students

    // Read details of 5 students
    for (int i = 0; i < 5; i++) {
        printf("Enter details for Student %d\n", i + 1);

        printf("Name: ");
        scanf("%49s", students[i].name);  // Prevent buffer overflow

        printf("Roll: ");
        scanf("%d", &students[i].roll_no);

        printf("Marks: ");
        scanf("%d", &students[i].marks);
    }

    // Print all student details in a tabular format
    printf("\n%-15s %-10s %-5s\n", "Name", "Roll", "Marks");
    printf("---------------------------------\n");
    for (int i = 0; i < 5; i++) {
        printf("%-15s %-10d %-5d\n", students[i].name, students[i].roll_no, students[i].marks);
    }

    return 0;
}
  
