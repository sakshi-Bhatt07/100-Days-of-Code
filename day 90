Q140: Define a struct with enum Gender and print person's gender.

/*
Sample Test Cases:
Input 1:
Gender=MALE
Output 1:
Male

*/
#include <stdio.h>

// Define an enum for Gender
typedef enum {
    MALE,
    FEMALE,
    OTHER
} Gender;

// Define a struct for Person
typedef struct {
    char name[50];
    Gender gender;
} Person;

int main() {
    // Create a person and assign gender
    Person p1;
    p1.gender = MALE;

    // Print gender using a simple switch or array
    switch (p1.gender) {
        case MALE:
            printf("Male\n");
            break;
        case FEMALE:
            printf("Female\n");
            break;
        case OTHER:
            printf("Other\n");
            break;
        default:
            printf("Unknown\n");
    }

    return 0;
}
  
